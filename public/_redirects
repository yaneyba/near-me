# SPA fallback for React Router
# Subdomain routing is now handled by Functions middleware
# This ensures non-subdomain requests fall back to the React app
!/index.html
!/assets/*
!/favicon*
!/robots.txt
!/sitemap.xml
!/*.html
/* /index.html 200