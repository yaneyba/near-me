# Cloudflare Pages redirects for path-based routing

# API routes (if any) - let them pass through
/api/* 200

# Static assets with extensions - let them pass through naturally
# SPA fallback - redirect all other routes to index.html (with force flag to prevent loops)
/* /index.html 200!
